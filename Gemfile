source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
gem "webrick", "~> 1.8"   # for local `jekyll serve` on Ruby 3
gem "tzinfo-data"         # only needed on Windows; harmless elsewhere
